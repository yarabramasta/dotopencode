# Android Fixture Workspace

Use the Android project at:

`~/Development/kopkarpay/member-app`

This repo contains the real Gradle/Kotlin app used for Android evals.

Suggested checks:

- review `build.gradle.kts`
- review `app/build.gradle.kts`
- review `settings.gradle.kts`
- inspect Kotlin sources under `app/src/main/java`
- use Gradle tasks only when the eval asks for them
