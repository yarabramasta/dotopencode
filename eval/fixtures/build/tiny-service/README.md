# Tiny Service Fixture

Minimal runnable service fixture for build evals.

Commands:

- `node --test`

Suggested eval tasks:

- add a small pure helper
- update tests for a tiny behavior change
- inspect implementation and explain a safe patch
